syntax = "proto3";

option go_package = "github.com/skrider/attainer/pb";

package pb;

message Chunk {
    string content = 1;
}

message Embedding {
    repeated float vec = 1;
}

service Model {
    rpc Predict(Chunk) returns (Embedding) {}
}

